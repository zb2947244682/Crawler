version: '3.8'

services:
  crawler-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./screenshots:/app/screenshots
    restart: unless-stopped
